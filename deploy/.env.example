# ClickHouse credentials
CLICKHOUSE_PASSWORD=changeme

# PostgreSQL credentials
POSTGRES_PASSWORD=changeme

# API auth signing secret. REQUIRED — the api container refuses to start without
# it. Generate a unique one: openssl rand -hex 32
JWT_SECRET=

# Docker image tag to deploy
TRACIUM_VERSION=latest
