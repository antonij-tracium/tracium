# Copy to .env and fill in real keys. The example scripts and the live
# conformance suite (tests/) call the real provider APIs, so these must be
# valid keys with billing enabled. Never commit the filled-in .env.
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_API_KEY=AIza...
