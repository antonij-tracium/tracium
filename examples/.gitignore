# Secrets — never commit. Copy .env.example to .env and fill in real keys.
.env
.env.*
!.env.example

# Python virtualenv and caches — recreate with: python -m venv venv && pip install -r requirements.txt
venv/
.venv/
__pycache__/
*.pyc
.pytest_cache/

# macOS
.DS_Store
